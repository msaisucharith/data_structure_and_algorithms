// set
// set is data type which stores only unique values
// insertion does not make on insertion order

// for creating set we will use constructure

const set = new Set([1, 2, 3, 5]);

// set methods

// add
set.add(20);

// has
set.has(20);

// delete

set.delete(20);

// size
console.log(set.size);

//
// set.clear();

console.log(set);
